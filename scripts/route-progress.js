import {state} from "./state.js";export function prepareRouteSteps(){let total=0;state.routeSteps=(state.routeSteps||[]).map((step,index)=>{const distance=Number(step.distance||0),out={...step,index,startDistance:total,endDistance:total+distance};total+=distance;return out});}