export function haversine(lat1,lng1,lat2,lng2){const r=6371000,toRad=v=>v*Math.PI/180,dLat=toRad(lat2-lat1),dLng=toRad(lng2-lng1);const a=Math.sin(dLat/2)**2+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;return r*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));}
export function projectPointToSegment(lat,lng,lat1,lng1,lat2,lng2){const mLat=111320,mLng=111320*Math.cos(lat*Math.PI/180),px=lng*mLng,py=lat*mLat,ax=lng1*mLng,ay=lat1*mLat,bx=lng2*mLng,by=lat2*mLat,dx=bx-ax,dy=by-ay,len=dx*dx+dy*dy;if(len===0)return{t:0,distanceMeters:Math.hypot(px-ax,py-ay)};let t=((px-ax)*dx+(py-ay)*dy)/len;t=Math.max(0,Math.min(1,t));return{t,distanceMeters:Math.hypot(px-(ax+t*dx),py-(ay+t*dy))};}
export function formatDistance(m){if(!Number.isFinite(m))return"—";if(m<1000)return`${Math.round(m)} m`;if(m<10000)return`${(m/1000).toFixed(1).replace(".",",")} km`;return`${Math.round(m/1000)} km`;}
export function formatDuration(s){if(!Number.isFinite(s))return"—";const min=Math.max(1,Math.round(s/60));if(min<60)return`${min} min`;const h=Math.floor(min/60),r=min%60;return r?`${h} t ${r} min`:`${h} t`;}
export function escapeHtml(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}
export function buildGoogleMapsLink(s){return`https://www.google.com/maps/search/?api=1&query=${s.lat},${s.lng}`;}
export function getPosition(){return new Promise(resolve=>{if(!navigator.geolocation){resolve({lat:55.6761,lng:12.5683,speed:0,heading:0});return}navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lng:p.coords.longitude,speed:typeof p.coords.speed==="number"?Math.max(0,p.coords.speed*3.6):0,heading:typeof p.coords.heading==="number"?p.coords.heading:0}),()=>resolve({lat:55.6761,lng:12.5683,speed:0,heading:0,demo:true}),{enableHighAccuracy:true,maximumAge:1000,timeout:9000});});}